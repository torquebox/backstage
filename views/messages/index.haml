#messages-index
  %h2 
    == #{simple_class_name( @destination ).capitalize}: #{link_to( object_path( @destination ), @destination.name)} :: Messages
  %table
    %tr
      %th ID
      %th Delivery Count
      %th Text
    - @destination.each do |message|
      %tr
        %td
          = message.jms_id 
        %td
          = message.delivery_count
        %td
          = truncate( message.text, 30 )
